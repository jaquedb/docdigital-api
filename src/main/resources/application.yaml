spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/docdigital
    username: postgres
    password: wellington

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    org.springframework.web: DEBUG

jwt:
  secret: docdigital-super-secret-key-2026

file:
  upload-dir: uploads
