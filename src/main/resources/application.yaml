spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/docdigital
    username: postgres
    password: wellington

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect